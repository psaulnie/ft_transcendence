export enum manageRoomsTypes {
	add = 0,
	remove,
	addDirectMsg
}
export enum sendMsgTypes {
	msg = 0,
}

export enum actionTypes {
	join = 0,
	left,
	kick,
	ban,
	private,
	msg,
	block,
	admin,
	mute,
	unmute,
	privmsg,
	wrongpassword,
	rightpassword,
	hasPassword,
	noPassword,
	invited
}