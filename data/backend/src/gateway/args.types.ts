
export enum sendMsgTypes {
	msg = 0,
	invite
}

export enum actionTypes {
	join = 0,
	left,
	kick,
	ban,
	private,
	msg,
	block,
	admin,
	mute,
	unmute,
	privmsg,
	wrongpassword,
	rightpassword,
	hasPassword,
	noPassword,
	invited,
	blockedmsg
}